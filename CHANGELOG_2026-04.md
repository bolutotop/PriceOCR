# 价格 OCR 改动报告（2026-04）

本次改动围绕 **密集多列烟价表 OCR 识别精度 + 前端未识别交互** 做了大量加固。
共 6 个文件变更，新增 ~1800 行，删除 ~100 行。

---

## 一、核心修复主线

针对用户反馈的一系列"未识别 / 错配 / 品名前缀丢失 / 切片不完整"等问题，
定位到多个根因并各自加固。

### 1. OCR 引擎层（`src/actions/ocr-engine-tencent.ts`）

#### 1.1 粘连拆分增强（`splitNameAndPrice`）

新增 / 修正的拆分规则：

| 规则 | 模式 | 示例 | 新增 |
|---|---|---|---|
| 0a | 剥离 "点烫" 后 "数字+汉字" | `"1230大峡谷情"` | 标注 `priceLeft=true` |
| 0b | "小数点价格 + 汉字品名" | `"131.5细支鸿运宽窄"` | 标注 `priceLeft=true` |
| **0c (新增)** | **"价格 + 空格 + 汉字品名"** | `"308.5 中人民大会堂"` | 跨列粘连新模式 |

返回值新增 `priceLeft?: boolean` 字段，`emitNameAndPrice` 据此决定左/右子块内容，
**修复 "价格在左、品名在右" 时子块 x 坐标倒置**（曾导致 `合至尊` 等条目抢不到同行价格）。

#### 1.2 `emitOne` 多列粘连拆分（`multiPairRe`）

针对 OCR 把 "品名A+价格A+品名B+价格B" 识别成一整串的情况
（如 `"银细支休闲禾1165细智圣出山575"`），在 `emitOne` 最前面检测并拆分。

约束矩阵（经多轮误伤修正后定稿）：

| 约束 | 作用 |
|---|---|
| `mp[1].length >= 2` | 避免 `"软95南京"` 的 `软` 被当作品名A |
| `mp[3].length >= 2` | 避免 `"合1906喜"` 的 `喜` 被当作品名B |
| 双对模式（`mp[4]` 存在）：`mp[2] >= 50` | 两个价格互相锚定，置信度高 |
| 单对模式（`mp[4]` 缺失）：`mp[2] >= 200` | 避免把 `"3克细95南京"` 的 `95` 误拆为价格 |

#### 1.3 OCR 原始块诊断

新增对关键测试词（南京、天叶、玉溪、商鼎、新版、合至尊、1045、163、204、276……）
的原始 `TextDetection` 打印，便于定位漏识 / 粘连 / 位置异常的源头。

---

### 2. 主流程（`src/actions/ocr.ts`）

#### 2.1 噪声过滤加强

新增 **绝对位置过滤**：
- `yCenter < 图高 × 3%` → 丢弃（页眉 / 店名 / 表头）
- `yCenter > 图高 × 98%` → 丢弃（页脚）

修复 `金花口(y=19)` 这类图顶噪声被当成品名且生成 `NO IMG` 切片的问题。

#### 2.2 2.5 水平合并：改为全局扫描

原先只合并 `extractedBlocks[i+1]`，因聚类错误（同行 block 全局索引不相邻）导致
`商鼎 + 新版` 这类该合并的被漏合。

**修改**：对每个非价格 `curr`，在整张图 y 同行范围内搜索 gap 最小的右邻文本块。
安全约束：
- `gap < H_avg × 0.6`（保守，不误合相邻列）
- `crossColumnRe` 检查：合并后出现"汉字+数字+汉字"（跨列特征）立即回滚

#### 2.3 3.B 配对：短修饰词吸并 + 全局兜底

**3.B 循环改进**：

a) **短修饰词吸并**：遇到下一个块是 "1~2 字、未字典命中" 时（如 `新版`、`点码`、`礼盒`），
   不再 break，而是合并进 `mergedName` 继续向右找价格；

b) **全局同行兜底**：3.B 的 sibling 扫描失败（`!foundPrice`）时，
   在 `extractedBlocks` 全局扫描 y 同行的未 used 价格块，按 `score = dy×10 + dx×0.1` 打分：
   - `yTolStrict = 0.6 × 行高`（严格同行）
   - x 必须在右侧且 `dx < MAX_X_DISTANCE`
   
c) **中间品名阻断守卫**（最新一轮）：若候选价格块与 `curr` 之间的 x 区间内还有
   未 used 的其它同行品名块，**放弃本次兜底**（避免像 `329软中` 跨品名抢下一列 1310）。
   宁可显示"未识别"让用户手动修，也不错配。

#### 2.4 3.6 字典兜底（新增）

对"字典命中 + 仍 `price=-1`"的条目，三轮扫描逐级放宽：
- **strict**（`ROW_TOL=0.75 字高`）
- **loose**（`ROW_TOL=1.2 字高`）
- **aggressive**（`ROW_TOL=1.8 字高` + 允许 orphan 中心在 name 中心左侧）

诊断信息保存到 `globalThis.__OCR_DICT_SALVAGE_DIAG__`，供 `/api/ocr-debug` 读取。

#### 2.5 3.7 二次局部 OCR 补救（新增）

对所有 `price=-1` 的条目（最多 15 条），crop 品名右侧区域单独调用 OCR，
救回全图 OCR 漏识的价格。典型案例：
- `中支天叶` 全图 OCR 没返回 `971`，crop 单独识别到 971 ✓
- `软95南京点码` 全图漏识 `702`，局部补救成功 ✓

#### 2.6 3.8 品名重复修正（新增）

对"同一品名出现 ≥2 次"的条目（如 OCR 把 `细蓝闪带王` 误识成 `细支品悦王`，
导致两行同名），对每个实例做品名区域 crop + 二次 OCR 重新识别真实品名。

安全守卫：
- 新品名 **不能比原品名短**（避免 crop 不全导致 `95南京 → 南京`）
- 新品名 **不能是原品名的后缀**（同上防御）

#### 2.7 `generateCrop` 切图 padding 优化

| 方向 | 原值 | 新值 | 动机 |
|---|---|---|---|
| 左 | 图宽 × 0.5% | **图宽 × 2.5%** | OCR polygon 常漏品名前缀（"细支"、"软"、"中支"） |
| 右 | 图宽 × 0.5% | 图宽 × 0.8% | 保守，避免吞到下一列 |
| 上下 | 图宽 × 0.5% | **行高 × 20%** | 按行高算而非图宽，更合理 |

#### 2.8 禁用字典纠错（按用户要求）

最终组装阶段和 3.8 二次修正都改为"**完全相信 OCR 原文**"：
- `finalName = pair.rawName`（不再使用纠错后名）
- `isCorrected = false`（不再显示纠错标签）
- **2.8 字典扫描仍保留**，仅用于 3.A 配对锚点判定，不影响展示

---

### 3. 前端（`src/app/import/page.tsx`）

#### 3.1 价格列表头"只看未识别"勾选

- 新增状态 `onlyUnrecognized`
- 表头右侧集成：勾选框 + "未识别" 文字 + 计数徽标
- 勾选后：仅展示 `price === -1` 行，且未识别置顶（JS 稳定排序）
- 未勾选：保持 OCR 原顺序
- `unrecognizedCount === 0` 时勾选框自动 disabled

#### 3.2 未识别视觉增强

价格输入框在 `price === -1` 时：
- 红色边框 + 浅红背景
- placeholder 改为 `未识别 ?`
- 左上角红色 `!` 徽章（悬浮提示 "OCR 未配到价格，请核对原图"）

#### 3.3 价格列宽调整

`w-[70px] sm:w-[140px]` → `w-[90px] sm:w-[180px]`，容纳勾选 UI。

---

### 4. 字典（`src/lib/corrector.ts`）

#### 4.1 自定义别名扩充

新增 2026-04 实测发现的漏网映射（10+ 条），含形近误识和词序颠倒：
- `锋芒七匹狼` → `七匹狼锋芒`
- `细支根蓝大能猫` → `典藏大熊猫`
- `翟长略` / `破长略` → `粗长乐好猫`
- `渝支印象` → `印象黄山` …

#### 4.2 长词阈值分级

| 长度 | 阈值 |
|---|---|
| ≤ 5 | 1（原有） |
| **6–7** | **2** |
| **≥ 8** | **3**（cam 倾斜图容差） |

---

### 5. 构建配置（`next.config.ts` / `.gitignore`）

- `next.config.ts`：
  - 迁移 `experimental.serverComponentsExternalPackages` → 顶层 `serverExternalPackages`（Next 16 兼容）
  - 新增 `outputFileTracingExcludes` 排除 `.vs/`、`.git/`、`.next/`，防止 Windows 文件占用
- `.gitignore`：新增 `.vs/`、`.idea/`、`.vscode/` IDE 目录

---

## 二、解决的已知问题列表

| # | 问题 | 修复手段 |
|---|---|---|
| 1 | `中支天叶` 未识别（OCR 漏识 971） | 3.7 二次局部 OCR |
| 2 | `合至尊` 缺价格（价格子块 x 倒置） | emitNameAndPrice 加 priceLeft |
| 3 | `商鼎 + 新版` 没合并成 `商鼎新版` | 2.5 改全局扫描 + 3.B 短修饰词吸并 |
| 4 | `细蓝闪带王` 被 OCR 误识为 `细支品悦王` | 3.8 品名重复修正 |
| 5 | `95南京` 前缀丢失（multiPairRe 误拆） | 单对模式阈值提到 200 + 二次 OCR 守卫 |
| 6 | `银细支休闲禾1165细智圣出山` 未拆分 | multiPairRe 双对 / 单对分级阈值 |
| 7 | `308.5 中人民大会堂` 类跨列粘连 | 新增 `splitNameAndPrice` 规则 0c |
| 8 | 切片左侧被截（品名前缀缺失） | generateCrop 左 padding 提到 2.5% 图宽 |
| 9 | `金花口` y=19 假条目 + NO IMG | 顶/底 3%/2% 区绝对过滤 |
| 10 | "未识别" 条目用户难发现 | 前端勾选 + 红框 + ! 徽章 |
| 11 | `329软中` 错配到下一列价格 1310 | 3.B 全局兜底中间品名阻断守卫 |

---

## 三、风险 & 回滚点

- 3.7 / 3.8 会额外调用 OCR（最多 ~20 次），单次识别耗时增加 ~2~3s。若后端 QPS 有限，
  可调低上限（当前 15 条）。
- 所有诊断 `console.log` 保留在生产代码中，方便追踪。可后续用环境变量开关。
- `globalThis.__OCR_DEBUG_*` 全局变量仅供调试路由 `/api/ocr-debug` 使用，
  每次请求末尾会清理，不会累积。

